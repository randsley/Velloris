# Core Audio & VAD
sounddevice>=0.4.6
pyaudio>=0.2.13
librosa>=0.10.0
torch>=2.0.0
numpy>=1.24.0
torchaudio>=2.0.0

# Model Engines
transformers>=4.36.0
accelerate>=0.25.0
sentencepiece>=0.1.99
einops>=0.7.0
langchain-community>=0.0.10
langchain>=0.1.0

# TTS Engine (Official Qwen3-TTS)
# Documentation: https://github.com/QwenLM/Qwen3-TTS
# Models: https://huggingface.co/collections/Qwen/qwen3-tts
qwen-tts>=0.1.0
soundfile>=0.12.0

# Optional: FlashAttention 2 for reduced GPU memory usage
# Install with: pip install flash-attn --no-build-isolation
# Only works with torch.float16 or torch.bfloat16 on compatible hardware

# STT Engine
openai-whisper>=20230314
ffmpeg-python>=0.2.0

# Testing (optional)
pytest>=7.4.0
pytest-asyncio>=0.21.0

# Hardware specific (Choose based on OS)
# For Windows: flash-attn >= 2.5.0, bitsandbytes
# For Mac: mlx, mlx-audio, mlx-lm
